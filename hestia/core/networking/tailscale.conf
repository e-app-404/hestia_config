# tailscale.conf — extracted_config (tailscale)
tailscale:
  magicdns: true
  # CGNAT range used by Tailscale
  cgnat_range: 100.64.0.0/10
  nodes:
    - id: ha_host
      hostname: homeassistant
      ts_hostname: homeassistant.reverse-beta.ts.net
      ts_ip: 100.105.130.99
    - id: client_belgium
      role: remote_client
      # details elided; captured at higher level

