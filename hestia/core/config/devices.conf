# devices.conf — extracted_config (devices)
devices:
  - id: rpi5
    role: home_assistant_host
    os: haos
    container_runtime: docker
    provides:
      - service_ref: ha_host#home_assistant_ui
      - service_ref: ha_host#smb_share

  - id: synology_ds220plus
    role: nas
    os: synology_dsm
    provides:
      - service_ref: synology_ds220plus#smb_share