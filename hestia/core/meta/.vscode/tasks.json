{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Ensure HA Mounts (macOS)",
            "type": "shell",
            "command": "${workspaceFolder}/hestia/tools/ha-mount-mac.sh",
            "problemMatcher": []
        }
    ]
}