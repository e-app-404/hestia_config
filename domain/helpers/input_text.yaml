# input_text.yaml
last_opened:
  name: "Text helper for last opened"
  initial: "Last opened"
  max: 100

last_motion:
  name: "Text helper for last motion"
  initial: "Last motion"
  max: 100

# Note: Large JSON storage helpers (tts_gate_registry, plex_tv_index, plex_movie_index)
# are created via UI to exceed 255 character YAML limit. See .storage/input_text
