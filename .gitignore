# Home Assistant runtime / noisy files
home-assistant_v2.db*
.storage/
.cloud/
deps/
.venv/
tmp/

# Hestia operational logs and temp
hestia/workspace/operations/logs/

# Prompt library outputs
hestia/library/prompts/logs/
hestia/library/prompts/reports/
*.db
*.db-shm
*.db-wal
home-assistant.log*
.HA_VERSION
.uuid
influx.cookie
icloud3.log

# Python artifacts
__pycache__/
*.pyc
# Editor / IDE
.vscode/
.idea/
.DS_Store
._*
# Backups / archives
backups/
backup*/
*.tar.gz
*.zip
# Exception: allow critical deployment bundles
!home_assistant_complete_restoration_v2.0_FINAL_APPROVED.tar.gz
# Exception: allow backup tools (not backup storage)
!hestia/tools/utils/backup/
# Secrets (do NOT commit)
secrets.yaml
secrets.yml
secrets.*.yaml

# Zone files contain geographical coordinates and personal location data
domain/zones/key_zones.yaml
domain/zones/*_zones.yaml
# Allow template files
!domain/zones/*.yaml.template

# Configuration files containing PII (IP addresses, MAC addresses, network topology)
# Device configurations - contain IP addresses, MAC addresses, device identifiers
hestia/config/devices/*.conf
hestia/config/devices/*.md
hestia/config/devices/*.json
# Network configurations - contain network topology, IP ranges, DNS info
hestia/config/network/*.conf
# System configurations - contain relationships, credentials references, topology
hestia/config/system/*.conf
hestia/config/system/*.toml
# Allow workspace configuration (no PII)
!hestia/config/system/hestia_workspace.conf
!hestia/config/system/hestia.toml
!hestia/config/system/cli.conf
# Preview configurations may contain generated credentials
hestia/config/preview/*.conf

# Remove SMB mounting scripts (contain PII)
hestia/tools/utils/samba/*.sh

# Ignore imported blueprints
hestia/library/templates/blueprints/*.yaml

# Guard against nested vendor repos/objects
**/.git/
# Allowlist: ensure canonical config stays tracked
!configuration.yaml
!automations.yaml
!scripts.yaml
!scenes.yaml
!packages/
!custom_components/
!blueprints/
!themes/
!www/
*.py[cod]
/.venv/
# Dev tooling ignores
.pytest_cache/
.mypy_cache/
/config
config2/*
tests/testing_config/deps
tests/testing_config/home-assistant.log*
# hass-release
data/
.token
# Translations
homeassistant/components/*/translations
# Hide sublime text stuff
*.sublime-project
*.sublime-workspace
# Hide some OS X stuff
.AppleDouble
.LSOverride
Icon
# Thumbnails
# IntelliJ IDEA
.idea
*.iml
# pytest
.pytest_cache
.cache
# GITHUB Proposed Python stuff:
__pycache__
# C extensions
*.so
# Packages
*.egg
*.egg-info
dist
build
eggs
.eggs
parts
bin
var
sdist
develop-eggs
.installed.cfg
lib
lib64
pip-wheel-metadata
# Logs
*.log
pip-log.txt
# Unit test / coverage reports
.coverage
coverage.xml
nosetests.xml
htmlcov/
test-reports/
test-results.xml
test-output.xml
pytest-*.txt
junit.xml
*.mo
# Mr Developer
.mr.developer.cfg
.project
.pydevproject
.python-version
.tool-versions
# emacs auto backups
*~
*#
*.orig
# venv stuff
pyvenv.cfg
pip-selfcheck.json
venv
.venv
Pipfile*
share/*
/Scripts/
# vimmy stuff
*.swp
*.swo
tags
ctags.tmp
# vagrant stuff
virtualization/vagrant/setup_done
virtualization/vagrant/.vagrant
virtualization/vagrant/config
# Visual Studio Code
.vscode/*

# Ignore generated artifacts/bundles (created by release/packaging scripts)
artifacts/
!.vscode/cSpell.json
!.vscode/extensions.json
!.vscode/tasks.json
.env
# Template patcher: ignore tools but keep scripts/patches
hestia/tools/template_patcher/tools/*
!hestia/tools/template_patcher/patch_ha_templates.sh
!hestia/tools/template_patcher/rollback_ha_templates.sh
!hestia/tools/template_patcher/patches/
!hestia/tools/template_patcher/scratch/
# Windows Explorer
desktop.ini
/home-assistant.pyproj
/home-assistant.sln
/.vs/*
# mypy
/.mypy_cache/*
/.dmypy.json
# Secrets
.lokalise_token
# monkeytype
monkeytype.sqlite3
# This is left behind by Azure Restore Cache
tmp_cache
# python-language-server / Rope
.ropeproject
# Will be created from script/split_tests.py
pytest_buckets.txt
# AI tooling
.claude
# Exclude logs and diagnostics
/logs/
*.err.log
*.out.log
# Shell tooling
/scripts/*.sh
/domain/shell_commands/*.sh
# Auto-generated Git sync logs
/config/hestia/diagnosticslogs/*.json
# Avoid committing sensitive config backups or archives
*.backup
# Custom components and WWW
/custom_components/
/www/
# Home Assistant .storage
/.storage/*
.storage/*
# --- Strategos cleanup • 2025-09-04 --------------------
# Fix: ignore Kyber/continuity artifacts at ANY depth
**/.kyb/
**/continuity/
# Fix: correct bad absolute path rule (repo-relative instead of /config/…)
# was: /config/hestia/diagnosticslogs/*.json
**/hestia/**/diagnosticslogs/*.json
# Quiet down Hestia prompt registry & transient diffs/hashes
hestia/**/prompt_registry/**
**/system_instruction_v2.2*.diff
**/diff.diff
**/hash
# Home Assistant runtime state & deps
home-assistant_v2.db*
# Optional: ignore entire hestia cache/work areas if they exist
hestia/cache/**
hestia/workspace/archive/vault/**
# -------------------------------------------------------
# --- Strategos HA cleanup (2025-09-04) --------------------------------
# Secrets and credentials
.git-credentials
.ssh/
# HA runtime/state (never track)
.homeassistant/
logs/
# HACS-managed content (never track)
custom_components/
www/
# TTS cache (large & volatile)
tts/
# Kyber/continuity artifacts anywhere
.kyb_minmerge/
# Reports/diagnostics and work areas
reports/
hestia/config/diagnostics/
hestia/work/
# Zigbee2MQTT runtime (keep config only)
zigbee2mqtt/database.db
zigbee2mqtt/state.json
zigbee2mqtt/coordinator_backup.json
# Misc local caches
.ps4-games.*
# Shell scripts — fix shallow rule to include subdirs
domain/shell_commands/**/*.sh
# Optional: ignore diffs/hashes you showed in status
**/*.diff
# Re-include lowercase 'scripts/' directory (override earlier /Scripts/ rule)
# Some systems (macOS) are case-insensitive so '/Scripts/' was matching 'scripts'.
!scripts/
!scripts/**
# Allow CI helper in bin
!bin/ci-meta-capture-enforce
# ----------------------------------------------------------------------
/# Allow hestia/reports/ to be tracked while still ignoring top-level reports/
!hestia/reports/
!hestia/reports/**
# Ignore generic reports/ at repo root (keep prior behavior)
reports/**
# Ignore any path segments containing legacy, deprecated, or obsolete
**/*legacy*/**
**/*deprecated*/**
**/*obsolete*/**
/.strategos/
/.git.DISABLED/
/.git.DISABLED.bak*/
/hestia/core/meta/.git.BAD*/
/.vscode
/.trash/
.git.backup-*
.storage/**
.cloud/**
.venv*/
deps/**
hestia/reports/**
.trash/**
.quarantine/**
artifacts/**
*.bk.*
*.tmp
# ADR-0018 canonical ignores
archive/**
!hestia/reports/*/

# Repo janitor: prevent symlink commits and ensure volatile data ignored
hestia/core/meta/gitdir-visible
hestia/reports/latest
tts/
www/
reports/

# Meta-Capture workspace artifacts (reports + JSONL index)
hestia/workspace/reports/meta_capture/
hestia/workspace/.hestia/index/
/config/hestia/workspace/staging/

# Absolute path variants to ensure ignores in any checkout context
/config/hestia/workspace/reports/meta_capture/
/config/hestia/workspace/.hestia/index/

# Personal path information that may contain PII
# Files containing hardcoded user paths, backup locations, or personal identifiers
**/backup_source_info.txt
**/validate_bundle.sh
# Exclude any files that might have been generated with personal paths
**/*evertappels*
**/HomeAssistant/_backups/*
/config

# build metadata
*.egg-info/
hestia/library/prompts/_archive/

# Archived personas should not be consumed by AI operators
hestia/library/docs/governance/personas/personas.archive/

# SQL database cache files and temporary promptsets
hestia/workspace/cache/out/sql_room_database_files/*.txt
hestia/workspace/cache/out/**/*.promptset.*
